[build-system]
requires = [
    "conan>=2.0.0",
    "setuptools",
    "scikit-build>=0.13",
    "cmake>=3.26",
    "ninja",
    "scikit-build-core >=0.4.3",
    "nanobind >=1.3.2"
]
build-backend = "scikit_build_core.build"
[project]
name = "cgalpy"
version = "1.0.1"
description = "CGAL Bindings"
readme = "README.md"
requires-python = ">=3.8"
authors = [
    { name = "radekaadek", email = "radekaadek@gmail.com" },
]
classifiers = [
    "License :: LGPL-3.0-or-later",
    "Programming Language :: Python :: 3",
]
# Optional: runtime dependency specification
# dependencies = [ "cryptography >=41.0" ]
[project.urls]
Homepage = "https://bitbucket.org/taucgl/cgal-python-bindings/src/master/"

[tool.scikit-build]
# Protect the configuration against future changes in scikit-build-core
minimum-version = "0.4"

# Setuptools-style build caching in a local directory
build-dir = "build/{wheel_tag}"

# Build stable ABI wheels for CPython 3.12+
wheel.py-api = "cp312"

[tool.cibuildwheel]
manylinux-x86_64-image = "radekaadek/cgalpy-manylinux_2_28_x86-64_image:v4"
# manylinux-i686-image = "manylinux_2_28"
# manylinux-aarch64-image = "manylinux_2_28"
# manylinux-ppc64le-image = "manylinux_2_28"
# manylinux-s390x-image = "manylinux_2_28"
build = "cp310-* cp311-* cp312-*"
# before-build = "echo $(pwd) && dnf install -y wget eigen3-devel mpfr-devel gmp-devel git && wget https://github.com/CGAL/cgal/archive/refs/tags/v6.0-beta1.tar.gz && tar -xvf v6.0-beta1.tar.gz && git clone --recurse-submodules https://github.com/wjakob/nanobind.git && wget https://archives.boost.io/release/1.86.0/source/boost_1_86_0.tar.gz && tar -xf boost_1_86_0.tar.gz && cd boost_1_86_0 && ./bootstrap.sh && ./b2 install --prefix=/usr/local && export Boost_INCLUDE_DIR=/usr/local/include && pip install conan && conan profile detect"

before-build = "pip install conan && conan profile detect"
environment = {CGAL_DIR="/project/cgal-6.0-beta1", nanobind_DIR="/project/nanobind", CGALPY_PYPI="1", PATH="/project/nanobind/include:$PATH"}

