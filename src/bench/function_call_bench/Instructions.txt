We compare four python bindings and a native implementation.
The four Python bindings are:
1. Boost.python
2. pybind11
3. nanobind
4. swig

Boost.python
------------
We use Boost version 1.78.
Install Boost

pybind11
--------
# Install from sources:
> git clone git@github.com:pybind/pybind11.git
# Assume <pybind11_DIR> points at the pybind11 build directory
> cd <pybind11_DIR>
# Assume <pybind11_SRC_DIR> points at the repository root directory
> cmake -DCMAKE_BUILD_TYPE=Release -DPYBIND11_TEST:BOOL=Off <pybind11_SRC_DIR>
# For some reason it does not work without installing, so
> sudo make install
# So there is no need to 'export pybind11_DIR=`pwd`'

nanobind
--------
# Install nanobind from sources:
git clone git@github.com:wjakob/nanobind.git
# Assume <nanobind_DIR> points at the nanobind build directory
> cd <nanobind_DIR>
# Assume <nanobind_SRC_DIR> points at the repository root directory
> cmake -DCMAKE_BUILD_TYPE=Release <nanobind_SRC_DIR>
> export nanobind_DIR=`pwd`

# Install tsl-robin-map from sources:
git clone git@github.com:Tessil/robin-map.git
# Assume <tsl-robin-map_DIR> points at the tsl-robin-map build directory
> cd <tsl-robin-map_DIR>
# Assume <tsl-robin-map_SRC_DIR> points at the repository root directory
> cmake -DCMAKE_BUILD_TYPE=Release <tsl-robin-map_SRC_DIR>
> export tsl_robin_map_DIR=`pwd`

swig
----
# install swig
> sudo apt install swig
