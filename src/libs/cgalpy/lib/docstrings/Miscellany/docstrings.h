// AUTO-GENERATED by cpp_docstrings_from_json.py — DO NOT EDIT
// Package: Miscellany
#ifndef CGAL_PYTHON_DOCSTRINGS_MISCELLANY_H
#define CGAL_PYTHON_DOCSTRINGS_MISCELLANY_H

namespace cgalpy {
namespace docstrings {
namespace Miscellany {

// CGAL::Handle_hash_function
constexpr const char* Handle_hash_function__class__ = "The class Handle_hash_function is a model for the UniqueHashFunction concept.";

constexpr const char* Handle_hash_function_Handle_hash_function = "Default constructor.";
constexpr const char* Handle_hash_function_operator = "Returns unique hash value for any Handle type for which &*key gives a unique address.

The type std::iterator_traits<Handle>::value_type has to be defined (which it
is already for pointers, handles, iterators, and circulators).

Parameters
----------
key : const Handle &

Returns: std::size_t";

// CGAL::Memory_sizer
constexpr const char* Memory_sizer__class__ = "The class Memory_sizer allows to measure the memory size used by the process.";

constexpr const char* Memory_sizer_Memory_sizer = "Default constructor.";
constexpr const char* Memory_sizer_virtual_size = "Returns the virtual memory size in bytes.

Returns: size_type";
constexpr const char* Memory_sizer_resident_size = "Returns the resident memory size in bytes.

Returns: size_type";

// CGAL::Modifier_base
constexpr const char* Modifier_base__class__ = "Modifier_base is an abstract base class providing the interface for any modifier.";

constexpr const char* Modifier_base_Representation = "the internal representation type.

Returns: R";
constexpr const char* Modifier_base_operator = "
rep is a valid representation.

Parameters
----------
rep : R &";

// CGAL::Profile_counter
constexpr const char* Profile_counter__class__ = "The class Profile_counter provides a way to count the number of times a given line of code is executed during the execution of a program, and print this number at the end of the execution of the program.";

constexpr const char* Profile_counter_Profile_counter = "The internal counter is initialized to 0, and the string s is stored for further printing by the destructor.

Parameters
----------
s : std::string";
constexpr const char* Profile_counter_Profile_counter_1 = "The value of the counter is printed to std::cerr together with the string.";
constexpr const char* Profile_counter_operator = "Increments the internal counter.";
constexpr const char* Profile_counter_CGAL_PROFILE = "Macro to enable and disable profiling statements.";
constexpr const char* Profile_counter_CGAL_PROFILER = "Profiling macro that uses CGAL::Profile_counter .

Parameters
----------
MSG";

// CGAL::Real_timer
constexpr const char* Real_timer__class__ = "The class Real_timer is a timer class for measuring real time.";

constexpr const char* Real_timer_Real_timer = "state is stopped .";
constexpr const char* Real_timer_start = "
state is stopped .";
constexpr const char* Real_timer_stop = "
state is running .";
constexpr const char* Real_timer_reset = "reset timer to zero.

The state is unaffected.";
constexpr const char* Real_timer_is_running = "true if the current state is running.

Returns: bool";
constexpr const char* Real_timer_time = "real time in seconds, or 0 if the underlying system call failed.

Returns: double";
constexpr const char* Real_timer_intervals = "number of start/stop-intervals since the last reset.

Returns: int";
constexpr const char* Real_timer_precision = "smallest possible time step in seconds, or -1 if the system call failed.

Returns: double";
constexpr const char* Real_timer_const = "maximal representable time in seconds.

Returns: double max";

// CGAL::Timer
constexpr const char* Timer__class__ = "The class Timer is a timer class for measuring user process time.";

constexpr const char* Timer_Timer = "state is stopped .";
constexpr const char* Timer_start = "
state is stopped .";
constexpr const char* Timer_stop = "
state is running .";
constexpr const char* Timer_reset = "reset timer to zero.

The state is unaffected.";
constexpr const char* Timer_is_running = "true if the current state is running.

Returns: bool";
constexpr const char* Timer_time = "user process time in seconds, or 0 if the underlying system call failed.

Returns: double";
constexpr const char* Timer_intervals = "number of start/stop-intervals since the last reset.

Returns: int";
constexpr const char* Timer_precision = "smallest possible time step in seconds, or -1 if the system call failed.

Returns: double";
constexpr const char* Timer_max = "maximal representable time in seconds.

Returns: double";

// CGAL::Union_find
constexpr const char* Union_find__class__ = "An instance P of the data type Union_find <T,A> is a partition of values of type T into disjoint sets.";

constexpr const char* Union_find_value_type = "values stored in items (equal to T ).

Returns: unspecified_type";
constexpr const char* Union_find_handle = "handle to values.

Returns: unspecified_type";
constexpr const char* Union_find_iterator = "iterator over values.

Returns: unspecified_type";
constexpr const char* Union_find_allocator = "allocator.

Returns: unspecified_type";
constexpr const char* Union_find_Union_find = "creates an instance P of type Union_find <T,A> and initializes it to the empty partition.";
constexpr const char* Union_find_get_allocator = "the allocator of the partition.

Returns: allocator";
constexpr const char* Union_find_number_of_sets = "returns the number of disjoint sets of the partition.

Returns: std::size_t";
constexpr const char* Union_find_size = "returns the number of values of the partition.

Returns: std::size_t";
constexpr const char* Union_find_bytes = "returns the memory consumed by the partition.

Returns: std::size_t";
constexpr const char* Union_find_size_1 = "returns the size of the set containing h .

Parameters
----------
h : const_handle

Returns: std::size_t";
constexpr const char* Union_find_clear = "reinitializes to an empty partition.";
constexpr const char* Union_find_make_set = "creates a new singleton set containing x and returns a handle to it.

Parameters
----------
x : const T &

Returns: handle";
constexpr const char* Union_find_push_back = "same as make_set() .

Parameters
----------
x : const T &

Returns: handle";
constexpr const char* Union_find_insert = "inserts the range of values referenced by [first,beyond) .

Forward_iterator must be a forward iterator with value type T .

Parameters
----------
first : Forward_iterator
beyond : Forward_iterator";
constexpr const char* Union_find_find = "returns a canonical handle of the set that contains h , i.e., P.same_set(h,h2) iff P.find(h) == P.find(h2) .

h is a handle in P .

Parameters
----------
p : const_handle

Returns: const_handle";
constexpr const char* Union_find_unify_sets = "unites the sets of partition P containing h1 and h2 .

h1 and h2 are in P .

Parameters
----------
h1 : handle
h2 : handle";
constexpr const char* Union_find_same_set = "returns true iff h1 and h2 belong to the same set of P .

h1 and h2 are in P .

Parameters
----------
h1 : const_handle
h2 : const_handle

Returns: bool";
constexpr const char* Union_find_begin = "returns an iterator pointing to the first value of the partition.

Returns: iterator";
constexpr const char* Union_find_end = "returns an iterator pointing beyond the last value of the partition.

Returns: iterator";

// CGAL::Unique_hash_map
constexpr const char* Unique_hash_map__class__ = "An instance of the class template Unique_hash_map is an injective mapping from the set of keys of type Key to the set of variables of type Data .";

constexpr const char* Unique_hash_map_Key = "the Key type.

Returns: unspecified_type";
constexpr const char* Unique_hash_map_Data = "the Data type.

Returns: unspecified_type";
constexpr const char* Unique_hash_map_Hash_function = "the unique hash function type.

Returns: unspecified_type";
constexpr const char* Unique_hash_map_Unique_hash_map = "creates an injective function from Key to the set of unused variables of type Data , sets default_data to default , passes the table_size as argument to the internal implementation, and initializes the hash function with fct .

Parameters
----------
default : const Data &
table_size : std::size_t
fct : const Hash_function &";
constexpr const char* Unique_hash_map_Unique_hash_map_1 = "creates an injective function from Key to the set of unused variables of type Data , sets default_data to default , passes the table_size as argument to the internal implementation, initializes the hash function with fct , and inserts all keys from the range [first1,beyond1) .

The data variable for each inserted key is initialized with the corresponding
value from the range [first2, first2 + (beyond1-first1)) . The increment
operator must be defined for values of type Key and for values of type Data .
beyond1 must be reachable from first1 using increments.

Parameters
----------
first1 : Key
beyond1 : Key
first2 : Data
default : const Data &
table_size : std::size_t
fct : const Hash_function &";
constexpr const char* Unique_hash_map_default_value = "the current default_value .

Returns: Data";
constexpr const char* Unique_hash_map_hash_function = "the current hash function.

Returns: Hash_function";
constexpr const char* Unique_hash_map_is_defined = "returns true if $ key$ is defined in *this .

Note that there can be keys defined that have not been inserted explicitly.
Their variables are initialized to default_value .

Parameters
----------
key : const Key &

Returns: bool";
constexpr const char* Unique_hash_map_reserve = "sets the table size.

Parameters
----------
table_size : std::size_t";
constexpr const char* Unique_hash_map_clear = "resets *this to the injective function from Key to the set of unused variables of type Data .

The default_data remains unchanged.";
constexpr const char* Unique_hash_map_clear_1 = "resets *this to the injective function from Key to the set of unused variables of type Data and sets default_data to default .

Parameters
----------
default : const Data &";
constexpr const char* Unique_hash_map_operator = "returns a reference to the variable map(key) .

If key has not been inserted into map before, key is inserted and initialized
with default_value .

Parameters
----------
key : const Key &

Returns: Data &";
constexpr const char* Unique_hash_map_operator_1 = "returns a const reference to the variable *this(key) .

If key has not been inserted into *this before, a const reference to the
default_value is returned. However, key is not inserted into *this .

Parameters
----------
key : const Key &

Returns: const Data &";
constexpr const char* Unique_hash_map_insert = "inserts all keys from the range [first1,beyond1) .

The data variable for each inserted key is initialized with the corresponding
value from the range [first2, first2 + (beyond1-first1)) . Returns first2 +
(beyond1-first1) . The increment operator must be defined for values of type
Key and for values of type Data . beyond1 must be reachable from first1 using
increments.

Parameters
----------
first1 : Key
beyond1 : Key
first2 : Data

Returns: Data";

// UniqueHashFunction
constexpr const char* UniqueHashFunction__class__ = "UniqueHashFunction is a concept for a hash function with unique hash values. An instance hash for a model of the UniqueHashFunction concept is a function object. It maps objects of its domain type Key to the integral image type std::size_t . The image values have to be unique for all keys in the domain type Key .";

constexpr const char* UniqueHashFunction_result_type = "type of the hash value.

Returns: std::size_t";
constexpr const char* UniqueHashFunction_UniqueHashFunction = "copy constructor.

Parameters
----------
hash2 : const UniqueHashFunction &";
constexpr const char* UniqueHashFunction_operator = "assignment.

Parameters
----------
hash2 : const UniqueHashFunction &

Returns: UniqueHashFunction &";
constexpr const char* UniqueHashFunction_operator_1 = "returns a unique hash value for the key value.

Parameters
----------
key : const Key &

Returns: std::size_t";

constexpr const char* Profile_counter_CGAL_PROFILE_1 = "Macro to enable and disable profiling statements.";
constexpr const char* Profile_counter_CGAL_PROFILER_1 = "Profiling macro that uses CGAL::Profile_counter .

Parameters
----------
MSG";

// std
constexpr const char* std__class__ = "STL namespace.";

} // namespace Miscellany
} // namespace docstrings
} // namespace cgalpy

#endif // CGAL_PYTHON_DOCSTRINGS_MISCELLANY_H
