// AUTO-GENERATED by cpp_docstrings_from_json.py — DO NOT EDIT
// Package: Polygon_mesh_processing
#ifndef CGAL_PYTHON_DOCSTRINGS_POLYGON_MESH_PROCESSING_H
#define CGAL_PYTHON_DOCSTRINGS_POLYGON_MESH_PROCESSING_H

namespace cgalpy {
namespace docstrings {
namespace Polygon_mesh_processing {

// CGAL::Polygon_mesh_processing::Principal_curvatures_and_directions
constexpr const char* Polygon_mesh_processing_Principal_curvatures_and_directions__class__ = "a struct for storing principal curvatures and directions.";

constexpr const char* Polygon_mesh_processing_Principal_curvatures_and_directions_min_curvature = "min curvature magnitude

Returns: GT::FT";
constexpr const char* Polygon_mesh_processing_Principal_curvatures_and_directions_max_curvature = "max curvature magnitude

Returns: GT::FT";
constexpr const char* Polygon_mesh_processing_Principal_curvatures_and_directions_min_direction = "min curvature direction vector

Returns: GT::Vector_3";
constexpr const char* Polygon_mesh_processing_Principal_curvatures_and_directions_max_direction = "max curvature direction vector

Returns: GT::Vector_3";

// CGAL::Polyhedral_envelope
constexpr const char* Polyhedral_envelope__class__ = "This class can be used to check if a query point, segment, or triangle is inside or outside a polyhedral envelope of a set of triangles, constructed for a given $ \\epsilon $ distance tolerance.";

constexpr const char* Polyhedral_envelope_Polyhedral_envelope = "Default constructor, envelope is empty";
constexpr const char* Polyhedral_envelope_Polyhedral_envelope_1 = "Constructor with a triangulated surface mesh.

TriangleMesh a model of VertexListGraph and FaceListGraph NamedParameters a
sequence of Named Parameters tmesh a triangle mesh epsilon the distance of the
Minkowski sum hull np an optional sequence of Named Parameters among the ones
listed below Optional Named Parameters <button class=\"collapsible\">
vertex_point_map </button> a property map associating points to the vertices
of tmesh Type: a class model of ReadablePropertyMap with
boost::graph_traits<PolygonMesh>::vertex_descriptor as key type and Point_3 as
value type Default: boost::get(CGAL::vertex_point, tmesh) Extra: If this
parameter is omitted, an internal property map for CGAL::vertex_point_t must
be available in TriangleMesh . <button class=\"collapsible\"> face_epsilon_map
</button> a property map associating to each face of tm an epsilon value Type:
a class model of ReadablePropertyMap with
boost::graph_traits<TriangleMesh>::face_descriptor as key type and double as
value type Default: Use epsilon for all faces The triangle mesh gets copied
internally, that is it can be modified after having passed as argument, while
the queries are performed

Parameters
----------
tmesh : const TriangleMesh &
epsilon : double
np : const NamedParameters &";
constexpr const char* Polyhedral_envelope_Polyhedral_envelope_2 = "Constructor using a subset of faces of a triangulated surface mesh.

FaceRange a model of ConstRange with FaceRange::const_iterator being a model
of InputIterator with boost::graph_traits<TriangleMesh>::face_descriptor as
value type TriangleMesh a model of FaceListGraph NamedParameters a sequence of
Named Parameters tmesh a triangle mesh face_range the subset of faces to be
considered when computing the polyhedron envelope epsilon the distance of the
Minkowski sum hull np an optional sequence of Named Parameters among the ones
listed below Optional Named Parameters <button class=\"collapsible\">
vertex_point_map </button> a property map associating points to the vertices
of tmesh Type: a class model of ReadablePropertyMap with
boost::graph_traits<PolygonMesh>::vertex_descriptor as key type and Point_3 as
value type Default: boost::get(CGAL::vertex_point, tmesh) Extra: If this
parameter is omitted, an internal property map for CGAL::vertex_point_t must
be available in TriangleMesh . <button class=\"collapsible\"> face_epsilon_map
</button> a property map associating to each face of tm an epsilon value Type:
a class model of ReadablePropertyMap with
boost::graph_traits<TriangleMesh>::face_descriptor as key type and double as
value type Default: Use epsilon for all faces The triangle mesh gets copied
internally, that is it can be modified after having passed as argument, while
the queries are performed

Parameters
----------
face_range : const FaceRange &
tmesh : const TriangleMesh &
epsilon : double
np : const NamedParameters &";
constexpr const char* Polyhedral_envelope_Polyhedral_envelope_3 = "Constructor with a triangle soup.

PointRange a model of the concept ConstRange with PointRange::const_iterator
being a model of InputIterator with a point as value type TriangleRange a
model of the concept ConstRange with TriangleRange::const_iterator being a
model of InputIterator whose value type is model of the concept
RandomAccessContainer whose value_type is convertible to std::size_t .
NamedParameters a sequence of Named Parameters points points of the soup of
triangles triangles each element in the range describes a triangle as a triple
of indices of the points in points epsilon the distance of the Minkowski sum
hull np an optional sequence of Named Parameters among the ones listed below
Optional Named Parameters <button class=\"collapsible\"> point_map </button> a
property map associating points to the elements of the range points Type: a
model of ReadablePropertyMap whose value type is Point_3 and whose key is the
value type of PointRange::const_iterator Default: CGAL::Identity_property_map
<button class=\"collapsible\"> face_epsilon_map </button> a property map
associating to each triangle an epsilon value Type: a class model of
ReadablePropertyMap with std::size_t as key type and double as value type
Default: Use epsilon for all triangles

Parameters
----------
points : const PointRange &
triangles : const TriangleRange &
epsilon : double
np : const NamedParameters &";
constexpr const char* Polyhedral_envelope_operator = "returns true , iff the query point is inside the polyhedral envelope.

Parameters
----------
query : const Point_3 &

Returns: bool";
constexpr const char* Polyhedral_envelope_operator_1 = "returns true , iff the query segment defined by the points source and target is inside the polyhedral envelope.

Parameters
----------
source : const Point_3 &
target : const Point_3 &

Returns: bool";
constexpr const char* Polyhedral_envelope_operator_2 = "returns true , iff the query triangle formed by the points t0 , t1 , and t2 is inside the polyhedral envelope.

Parameters
----------
t0 : const Point_3 &
t1 : const Point_3 &
t2 : const Point_3 &

Returns: bool";
constexpr const char* Polyhedral_envelope_operator_3 = "returns true , iff all the triangles of tmesh are inside the polyhedral envelope.

TriangleMesh a model of FaceListGraph NamedParameters a sequence of Named
Parameters tmesh a triangle mesh np an optional sequence of Named Parameters
among the ones listed below Optional Named Parameters <button
class=\"collapsible\"> vertex_point_map </button> a property map associating
points to the vertices of tmesh Type: a class model of ReadablePropertyMap
with boost::graph_traits<PolygonMesh>::vertex_descriptor as key type and
Point_3 as value type Default: boost::get(CGAL::vertex_point, tmesh) Extra: If
this parameter is omitted, an internal property map for CGAL::vertex_point_t
must be available in TriangleMesh . Todo Add ConcurrencyTag as template
parameter + use TBB parallel for Find a way to test the containment of the
vertices first and then the triangles. It requires to have a map vertex->prism
id so that we can test if the 3 vertices of a face are in the same face + have
the initial list of prisms. apply that to the soup versions

Parameters
----------
tmesh : const TriangleMesh &
np : const NamedParameters &

Returns: bool";
constexpr const char* Polyhedral_envelope_operator_4 = "returns true , iff all the triangles in triangles are inside the polyhedral envelope.

PointRange a model of the concept ConstRange with PointRange::const_iterator
being a model of InputIterator with a point as value type TriangleRange a
model of the concept ConstRange with TriangleRange::const_iterator being a
model of InputIterator whose value type is model of the concept
RandomAccessContainer whose value_type is convertible to std::size_t .
NamedParameters a sequence of Named Parameters points points of the soup of
triangles triangles each element in the range describes a triangle as a triple
of indices of the points in points np an optional sequence of Named Parameters
among the ones listed below Optional Named Parameters <button
class=\"collapsible\"> point_map </button> a property map associating points to
the elements of the range points Type: a model of ReadablePropertyMap whose
value type is Point_3 and whose key is the value type of
PointRange::const_iterator Default: CGAL::Identity_property_map

Parameters
----------
points : const PointRange &
triangles : const TriangleRange &
np : const NamedParameters &

Returns: bool";
constexpr const char* Polyhedral_envelope_operator_5 = "returns true , iff all the triangles in triangle_range are inside the polyhedral envelope.

TriangleRange a model of ConstRange with TriangleRange::const_iterator being a
model of InputIterator with a value type being itself a model of ConstRange
whose const_iterator is a model of InputIterator with Point_3 as value type.
triangle_range a range of triangles

Parameters
----------
triangle_range : const TriangleRange &

Returns: bool";
constexpr const char* Polyhedral_envelope_is_empty = "returns true if the polyhedral envelope is empty, and false otherwise.

Returns: bool";







// CGAL::Rigid_triangle_mesh_collision_detection
constexpr const char* Rigid_triangle_mesh_collision_detection__class__ = "This class provides methods to perform some intersection tests between triangle meshes that undergo affine transformations (rotation, translation, and scaling).";

constexpr const char* Rigid_triangle_mesh_collision_detection_reserve = "increases the capacity of data structures used internally, size being the number of meshes expected to be added.

Parameters
----------
size : std::size_t";
constexpr const char* Rigid_triangle_mesh_collision_detection_remove_mesh = "removes the mesh with id mesh_id from the set, the indices of other meshes are kept unchanged.

Parameters
----------
mesh_id : std::size_t";
constexpr const char* Rigid_triangle_mesh_collision_detection_size = "returns the number of meshes in the set

Returns: std::size_t";
constexpr const char* Rigid_triangle_mesh_collision_detection_AABB_tree = "The AABB_tree type representing the triangles of each input mesh.

Returns: unspecified_type";
constexpr const char* Rigid_triangle_mesh_collision_detection_Vertex_point_map = "The vertex point map type used with TriangleMesh .

Returns: unspecified_type";
constexpr const char* Rigid_triangle_mesh_collision_detection_Point_3 = "Point type.

Returns: boost::property_traits< Vertex_point_map >::value_type";
constexpr const char* Rigid_triangle_mesh_collision_detection_Rigid_triangle_mesh_collision_detection = "move constructor

Parameters
----------
other : Rigid_triangle_mesh_collision_detection &&";
constexpr const char* Rigid_triangle_mesh_collision_detection_operator = "move assignment operator

Parameters
----------
other : Rigid_triangle_mesh_collision_detection &&

Returns: Rigid_triangle_mesh_collision_detection &";
constexpr const char* Rigid_triangle_mesh_collision_detection_add_mesh = "adds mesh tm to the set of meshes to be considered for intersection.

NamedParameters a sequence of Named Parameters tm triangulated surface mesh to
add np an optional sequence of Named Parameters among the ones listed below
Optional Named Parameters <button class=\"collapsible\"> vertex_point_map
</button> a property map associating points to the vertices of tm Type: a
class model of ReadablePropertyMap with
boost::graph_traits<TriangleMesh>::vertex_descriptor as key type and Point_3
as value type Default: boost::get(CGAL::vertex_point, tm) Extra: If this
parameter is omitted, an internal property map for CGAL::vertex_point_t should
be available for the vertices of tm . <button class=\"collapsible\">
face_index_map </button> a property map associating to each face of tm a
unique index between 0 and num_faces(tm) - 1 Type: a class model of
ReadablePropertyMap with boost::graph_traits<PolygonMesh>::face_descriptor as
key type and std::size_t as value type Default: an automatically indexed
internal map <button class=\"collapsible\"> apply_per_connected_component
</button> If false , tm is assumed to have only one connected component,
avoiding the extraction of connected components. Type: Boolean Default: true
the id of tm used to refer to that mesh.

Parameters
----------
tm : const TriangleMesh &
np : const NamedParameters &

Returns: std::size_t";
constexpr const char* Rigid_triangle_mesh_collision_detection_add_mesh_1 = "adds an instance of a triangulated surface mesh using an external tree of its faces.

The tree is not copied and the lifetime of tree must be longer than that of
this class. NamedParameters a sequence of Named Parameters tree an AABB-tree
of faces of a mesh tm triangulated surface mesh np an optional sequence of
Named Parameters among the ones listed below Optional Named Parameters <button
class=\"collapsible\"> vertex_point_map </button> a property map associating
points to the vertices of tm Type: a class model of ReadWritePropertyMap with
boost::graph_traits<TriangleMesh>::vertex_descriptor as key type and Point_3
as value type Default: boost::get(CGAL::vertex_point, tm) Extra: If this
parameter is omitted, an internal property map for CGAL::vertex_point_t should
be available for the vertices of tm . <button class=\"collapsible\">
face_index_map </button> a property map associating to each face of tm a
unique index between 0 and num_faces(tm) - 1 Type: a class model of
ReadablePropertyMap with boost::graph_traits<PolygonMesh>::face_descriptor as
key type and std::size_t as value type Default: an automatically indexed
internal map <button class=\"collapsible\"> apply_per_connected_component
</button> If false , tm is assumed to have only one connected component,
avoiding the extraction of connected components Type: Boolean Default: true
the id of tm used to refer to that mesh.

Parameters
----------
tree : const AABB_tree &
tm : const TriangleMesh &
np : const NamedParameters &

Returns: std::size_t";
constexpr const char* Rigid_triangle_mesh_collision_detection_set_transformation = "sets the transformation associated to a mesh identified by its id in the set.

Parameters
----------
mesh_id : std::size_t
aff_trans : const Aff_transformation_3< K > &";
constexpr const char* Rigid_triangle_mesh_collision_detection_get_all_intersections = "returns a vector of the ids of meshes within ids that have at least a face intersecting a face of the mesh with id mesh_id .

If mesh_id is in ids , it is not reported. MeshIdRange a range of ids
convertible to std::size_t .

Parameters
----------
mesh_id : std::size_t
ids : const MeshIdRange &

Returns: std::vector< std::size_t >";
constexpr const char* Rigid_triangle_mesh_collision_detection_get_all_intersections_1 = "returns a vector of the ids of meshes in the set that have at least a face intersecting a face of the mesh with id mesh_id

Parameters
----------
mesh_id : std::size_t

Returns: std::vector< std::size_t >";
constexpr const char* Rigid_triangle_mesh_collision_detection_get_all_intersections_and_inclusions = "returns a vector of the ids of meshes within ids that are intersecting with the mesh with id mesh_id , considering volume inclusions for closed meshes.

More precisely, if at least one face of a mesh with id i intersects a face of
the mesh with id mesh_id , the pair (i, false) is put in the output vector. If
there is no face intersection, but at least one of the meshes with ids i and
mesh_id is closed, and at least one connected component is included in the
bounded volume defined by a closed mesh then the pair (i, true) is put in the
output vector (independently of mesh i or mesh_id being the one including the
other). The inclusion test is done using Side_of_triangle_mesh , in particular
surface orientation is ignored and only the nesting level of connected
components defines a bounded volume. If a mesh has some self-intersection the
inclusion test may return incorrect results. If mesh_id is in ids it is not
reported. MeshIdRange a range of ids convertible to std::size_t . If a mesh is
made of several connected components and at least one component is not closed,
then no inclusion test will be made even if some components are closed.

Parameters
----------
mesh_id : std::size_t
ids : const MeshIdRange &

Returns: std::vector< std::pair< std::size_t, bool > >";
constexpr const char* Rigid_triangle_mesh_collision_detection_get_all_intersections_and_inclusions_1 = "returns a vector of the ids of meshes in the set that are intersecting with the mesh with id mesh_id , considering volume inclusions for closed meshes.

See the previous overload for details.

Parameters
----------
mesh_id : std::size_t

Returns: std::vector< std::pair< std::size_t, bool > >";

// CGAL::Side_of_triangle_mesh
constexpr const char* Side_of_triangle_mesh__class__ = "This class provides an efficient point location functionality with respect to a domain bounded by one or several disjoint closed triangle meshes.";

constexpr const char* Side_of_triangle_mesh_AABB_tree = "AABB-tree accepting faces of TriangleMesh .

Returns: unspecified_type";
constexpr const char* Side_of_triangle_mesh_Side_of_triangle_mesh = "Constructor with one triangulated surface mesh.

tmesh the triangulated surface mesh bounding the domain to be tested vpmap the
property map with the points associated to the vertices of tmesh gt an
instance of the geometric traits class CGAL::is_closed(tmesh)
CGAL::is_triangle_mesh(tmesh)

Parameters
----------
tmesh : const TriangleMesh &
vpmap : VertexPointMap
gt : const GeomTraits &";
constexpr const char* Side_of_triangle_mesh_Side_of_triangle_mesh_1 = "Constructor with one surface triangle mesh, using get(boost::vertex_point, tmesh) as vertex point property map.

tmesh the triangulated surface mesh bounding the domain to be tested gt an
instance of the geometric traits class CGAL::is_closed(tmesh)
CGAL::is_triangle_mesh(tmesh)

Parameters
----------
tmesh : const TriangleMesh &
gt : const GeomTraits &";
constexpr const char* Side_of_triangle_mesh_Side_of_triangle_mesh_2 = "Constructor that takes a pre-built CGAL AABB_tree of the triangulated surface mesh primitives.

tree a CGAL AABB_tree with AABB_face_graph_triangle_primitive as Primitive
type gt an instance of the geometric traits class tree contains a set of
triangle faces representing a closed surface mesh

Parameters
----------
tree : const AABB_tree &
gt : const GeomTraits &";
constexpr const char* Side_of_triangle_mesh_Side_of_triangle_mesh_3 = "Constructor that takes a pre-built CGAL AABB_tree of the triangulated surface mesh primitives, and moves it.

tree a CGAL AABB_tree with AABB_face_graph_triangle_primitive as Primitive
type gt an instance of the geometric traits class tree contains a set of
triangle faces representing a closed surface mesh

Parameters
----------
tree : AABB_tree &&
gt : const GeomTraits &";
constexpr const char* Side_of_triangle_mesh_Side_of_triangle_mesh_4 = "Constructor moving an instance of Side_of_triangle_mesh to a new memory location with minimal memory copy.

other the instance to be moved

Parameters
----------
other : Side_of_triangle_mesh &&";
constexpr const char* Side_of_triangle_mesh_operator = "Assign operator moving an instance of Side_of_triangle_mesh to this location with minimal memory copy.

other the instance to be moved a reference to this

Parameters
----------
other : Side_of_triangle_mesh &&

Returns: Side_of_triangle_mesh &";
constexpr const char* Side_of_triangle_mesh_operator_1 = "returns the location of a query point

point the query point to be located with respect to the input polyhedral
surface CGAL::ON_BOUNDED_SIDE if the point is inside the volume bounded by the
input triangle mesh CGAL::ON_BOUNDARY if the point is on triangle mesh
CGAL::ON_UNBOUNDED_SIDE if the point is outside triangle mesh

Parameters
----------
point : const Point &

Returns: Bounded_side";



// PMPDistanceTraits
constexpr const char* PMPDistanceTraits__class__ = "The concept PMPDistanceTraits is a refinement of the concepts AABBGeomTraits_3 and SpatialSortingTraits_3 . In addition to the types required by those concepts, it also requires types and functors needed by the functions approximate_max_distance_to_point_set() , sample_triangle_mesh() , approximate_Hausdorff_distance() and max_distance_to_triangle_mesh() .";

constexpr const char* PMPDistanceTraits_Compute_squared_area_3 = "Functor for computing squared area of a triangle.

It provides FT operator()(const Point_3 &, const Point_3 &, const Point_3 &)
const and FT operator()(const Triangle_3&) const and has FT as result_type.

Returns: unspecified_type";
constexpr const char* PMPDistanceTraits_Compute_squared_length_3 = "Functor for computing squared length of a segment.

and FT operator()(const Segment_3&) const and has FT as result_type.

Returns: unspecified_type";
constexpr const char* PMPDistanceTraits_Construct_translated_point_3 = "Functor for constructing translated points.

It provides Point_3 operator()(const Point_3 &, const Vector_3 &)

Returns: unspecified_type";
constexpr const char* PMPDistanceTraits_Construct_vector_3 = "Functor for constructing vectors.

It provides Vector_3 operator()(const Point_3 &, const Point_3 &)

Returns: unspecified_type";
constexpr const char* PMPDistanceTraits_Construct_scaled_vector_3 = "Functor for constructing scaled vectors.

It provides Vector_3 operator()(const Vector_3 &, const FT &)

Returns: unspecified_type";
constexpr const char* PMPDistanceTraits_FT = "A number type model of Field and RealEmbeddable .

Returns: unspecified_type";
constexpr const char* PMPDistanceTraits_Point_3 = "3D point type It must be default constructible, and can be constructed from 3 objects of type FT .

bool operator<( Point_3 , Point_3 ) to lexicographically compare two points
must be available. Access to Cartesian coordinates must be possible using
Point_3::x() , Point_3::y(), Point_3::z() and FT operator[](int i) with 0 <= i
< 3 . There must be a specialization of CGAL::Kernel_traits such that
CGAL::Kernel_traits< Point_3 >::Kernel is a model implementing this concept.

Returns: unspecified_type";
constexpr const char* PMPDistanceTraits_Vector_3 = "3D vector type

Returns: unspecified_type";

// PMPHolefillingVisitor
constexpr const char* PMPHolefillingVisitor__class__ = "The concept PMPHolefillingVisitor defines the requirements for the visitor used in hole-filling-related functions . The hole filling may use a 2D constrained triangulation for almost planar holes ( planar phase ). If that is not appropriate or fails it may use an algorithm with a quadratic running time relying on the 3D Delaunay triangulation ( quadratic phase ). If that fails, it uses an algorithm with cubic running time ( cubic phase ).";

constexpr const char* PMPHolefillingVisitor_start_planar_phase = "called when the planar hole filling algorithm starts.";
constexpr const char* PMPHolefillingVisitor_end_planar_phase = "called when the planar hole filling algorithm stops.

success true when the hole could be filled in this phase.

Parameters
----------
success : bool";
constexpr const char* PMPHolefillingVisitor_start_quadratic_phase = "called when the algorithm with quadratic running time starts.

n the upperbound on the number of steps

Parameters
----------
n : std::size_t";
constexpr const char* PMPHolefillingVisitor_quadratic_step = "called at each step.

There may be less than n calls as this is an upperbound.";
constexpr const char* PMPHolefillingVisitor_end_quadratic_phase = "called when the algorithm with quadratic running time ends.

success true when the hole could be filled in this phase.

Parameters
----------
success : bool";
constexpr const char* PMPHolefillingVisitor_start_cubic_phase = "called when the algorithm with cubic running time starts.

n the upperbound on the number of steps

Parameters
----------
n : std::size_t";
constexpr const char* PMPHolefillingVisitor_cubic_step = "called at each step. This will be called n times.";
constexpr const char* PMPHolefillingVisitor_end_cubic_phase = "called when the algorithm with cubic running time ends.";
constexpr const char* PMPHolefillingVisitor_start_refine_phase = "called before refining the triangulation of the hole ( triangulate_and_refine_hole() and triangulate_refine_and_fair_hole() only)";
constexpr const char* PMPHolefillingVisitor_end_refine_phase = "called after having refined the triangulation of the hole ( triangulate_and_refine_hole() and triangulate_refine_and_fair_hole() only)";
constexpr const char* PMPHolefillingVisitor_start_fair_phase = "called before fairing the triangulation of the hole ( triangulate_refine_and_fair_hole() only)";
constexpr const char* PMPHolefillingVisitor_end_fair_phase = "called after having faired the triangulation of the hole ( triangulate_refine_and_fair_hole() only)";
constexpr const char* PMPHolefillingVisitor_accept_triangle = "called before considering a triangle made from the points with indices i0, i1, and i2 to triangulate a hole.

The indices refers to the position of the points in the input point range if
triangulating a polyline, or to the position of the vertices along the cycle
of halfedges of the input border halfedge if triangulating a hole in a polygon
mesh ( 0 being the target of the input border halfedge, 1 the target of the
next border halfedge, ...). If false is returned, the triangle will not be
used. If true is returned, the triangle will be added to the candidate
triangles.

Parameters
----------
i0 : int
i1 : int
i2 : int

Returns: bool";

// PMPPolygonSoupOrientationVisitor
constexpr const char* PMPPolygonSoupOrientationVisitor__class__ = "The concept PMPPolygonSoupOrientationVisitor defines the requirements for the visitor used in CGAL::Polygon_mesh_processing::orient_polygon_soup() to track the detection of non-manifold simplices and the modifications performed to polygons during the orientation process.";

constexpr const char* PMPPolygonSoupOrientationVisitor_non_manifold_edge = "called each time an edge appears in more than two polygons.

id1 and id2 are the vertex ids of the endpoints of the edge. nb_polygons
indicates the number of polygons containing that edge.

Parameters
----------
id1 : std::size_t
id2 : std::size_t
nb_polygons : std::size_t";
constexpr const char* PMPPolygonSoupOrientationVisitor_non_manifold_vertex = "called each time a non-manifold vertex is detected.

vid is the id of the vertex that is non-manifold. nb_link_ccs is the number of
edge connected components of vertices in the link of the corresponding vertex.

Parameters
----------
vid : std::size_t
nb_link_ccs : std::size_t";
constexpr const char* PMPPolygonSoupOrientationVisitor_link_connected_polygons = "called during the detection of a non-manifold vertex, one time per incident edge connected component of vertices in the link of the vertex with id vid .

id is the id of the vertex that is non-manifold. polygon_ids contains the ids
of the polygon in such a connected component. This function is called a number
of times exactly equal to the parameter nb_link_ccs for the same vertex in
non_manifold_vertex() . Note that the aforementioned function is called after
all the calls to this function are done.

Parameters
----------
vid : std::size_t
polygon_ids : const std::vector< std::size_t > &";
constexpr const char* PMPPolygonSoupOrientationVisitor_polygon_orientation_reversed = "called when the orientation of a polygon is not compatible with its neighbors and the polygon is reversed.

id is the index of the polygon in the input polygon range.

Parameters
----------
id : std::size_t";
constexpr const char* PMPPolygonSoupOrientationVisitor_duplicated_vertex = "called for each non-manifold vertex (whether part of a non-manifold edge or not).

Non-manifoldness is resolved in the algorithm by duplicating the vertex.
input_id is the index of the input vertex, and new_id is the index of the new
vertex. Note that a vertex might be duplicated several times.

Parameters
----------
input_id : std::size_t
new_id : std::size_t";
constexpr const char* PMPPolygonSoupOrientationVisitor_vertex_id_in_polygon_replaced = "called when the vertex with id input_id in polygon with id pid is replaced by the vertex with id new_id .

This function is called after all calls to duplicated_vertex() .

Parameters
----------
pid : std::size_t
input_id : std::size_t
new_id : std::size_t";

// PMPSelfIntersectionTraits
constexpr const char* PMPSelfIntersectionTraits__class__ = "Geometric traits concept for the functions CGAL::self_intersections() and CGAL::does_self_intersect() .";

constexpr const char* PMPSelfIntersectionTraits_Point_3 = "3D point type

Returns: unspecified_type";
constexpr const char* PMPSelfIntersectionTraits_Triangle_3 = "3D triangle type

Returns: unspecified_type";
constexpr const char* PMPSelfIntersectionTraits_Segment_3 = "3D segment type

Returns: unspecified_type";
constexpr const char* PMPSelfIntersectionTraits_Construct_triangle_3 = "Functor constructing triangles.

It provides Triangle_3 operator() const(const Point_3 &, const Point_3 &,
const Point_3 &)

Returns: unspecified_type";
constexpr const char* PMPSelfIntersectionTraits_Construct_segment_3 = "Functor constructing segments.

It provides Segment_3 operator() const(const Point_3 &, const Point_3 &)

Returns: unspecified_type";
constexpr const char* PMPSelfIntersectionTraits_Do_intersect_3 = "Functor testing intersections between triangles and segments.

It provides bool operator() const (const Triangle_3 &, const Segment_3 &) and
bool operator() const (const Triangle_3 &, const Triangle_3 &)

Returns: unspecified_type";

// PMPSizingField
constexpr const char* PMPSizingField__class__ = "The concept PMPSizingField defines the requirements for the sizing field used in CGAL::Polygon_mesh_processing::isotropic_remeshing() to define the target length for every individual edge during the remeshing process.";

constexpr const char* PMPSizingField_vertex_descriptor = "Vertex descriptor type.

Returns: boost::graph_traits< PolygonMesh >::vertex_descriptor";
constexpr const char* PMPSizingField_halfedge_descriptor = "Halfedge descriptor type.

Returns: boost::graph_traits< PolygonMesh >::halfedge_descriptor";
constexpr const char* PMPSizingField_Point_3 = "3D point type matching the value type of the vertex property map passed to CGAL::Polygon_mesh_processing::isotropic_remeshing()

Returns: unspecified_type";
constexpr const char* PMPSizingField_PolygonMesh = "Polygon mesh type matching the type passed to CGAL::Polygon_mesh_processing::isotropic_remeshing() .

Returns: unspecified_type";
constexpr const char* PMPSizingField_FT = "Number type matching the FT type of the geometric traits passed to CGAL::Polygon_mesh_processing::isotropic_remeshing() .

Returns: unspecified_type";
constexpr const char* PMPSizingField_at = "returns the sizing value at v (used during tangential relaxation).

Parameters
----------
v : const vertex_descriptor
pmesh : const PolygonMesh &

Returns: FT";
constexpr const char* PMPSizingField_is_too_long = "returns the ratio of the current edge squared length and the local target edge squared length between the points of va and vb in case the current edge is too long, and std::nullopt otherwise (used for triggering edge splits and preventing some edge collapses).

Parameters
----------
va : const vertex_descriptor
vb : const vertex_descriptor
pmesh : const PolygonMesh &

Returns: std::optional< FT >";
constexpr const char* PMPSizingField_is_too_short = "returns the ratio of the squared length of h and the local target edge squared length if it is too short, and std::nullopt otherwise (used for triggering edge collapses).

Parameters
----------
h : const halfedge_descriptor
pmesh : const PolygonMesh &

Returns: std::optional< FT >";
constexpr const char* PMPSizingField_split_placement = "returns the position of the new vertex created when splitting the edge of h .

Parameters
----------
h : const halfedge_descriptor
pmesh : const PolygonMesh &

Returns: Point_3";
constexpr const char* PMPSizingField_register_split_vertex = "function called after the addition of the split vertex v in pmesh .

This function can be used for example to update a pre-computed sizing field.

Parameters
----------
v : const vertex_descriptor
pmesh : const PolygonMesh &";

// PMPTriangulateFaceVisitor
constexpr const char* PMPTriangulateFaceVisitor__class__ = "The concept PMPTriangulateFaceVisitor defines the requirements for the visitor used in triangulation-related functions to track the creation of new faces.";

constexpr const char* PMPTriangulateFaceVisitor_before_subface_creations = "called before the triangulation of f_split .

Note that f_split will be one of the faces of the triangulation. Each
subsequent call to after_subface_created() will correspond to the creation of
a new face of the triangulation of f_split .

Parameters
----------
f_split : face_descriptor";
constexpr const char* PMPTriangulateFaceVisitor_after_subface_creations = "called when the triangulation of a face in tm is finished.";
constexpr const char* PMPTriangulateFaceVisitor_after_subface_created = "called after creating a new triangle face f_new to triangulate the face passed to before_subface_creations() .

Note that the call is placed just after a call to add_face() so the halfedge
pointer is not set yet.

Parameters
----------
f_new : face_descriptor";
constexpr const char* PMPTriangulateFaceVisitor_accept_face = "called before considering a triangle made of vertices (v0,v1,v2) to triangulate f .

If false is returned, the triangle will not be used. If true is returned, the
triangle will be added to the candidate triangles.

Parameters
----------
f : face_descriptor
v0 : vertex_descriptor
v1 : vertex_descriptor
v2 : vertex_descriptor

Returns: bool";
constexpr const char* PMPTriangulateFaceVisitor_face_descriptor = "Face descriptor type.

Returns: unspecified_type";
constexpr const char* PMPTriangulateFaceVisitor_vertex_descriptor = "Vertex descriptor type.

Returns: unspecified_type";


// std
constexpr const char* std__class__ = "STL namespace.";

} // namespace Polygon_mesh_processing
} // namespace docstrings
} // namespace cgalpy

#endif // CGAL_PYTHON_DOCSTRINGS_POLYGON_MESH_PROCESSING_H
