// AUTO-GENERATED by cpp_docstrings_from_json.py — DO NOT EDIT
// Package: Point_set_3
#ifndef CGAL_PYTHON_DOCSTRINGS_POINT_SET_3_H
#define CGAL_PYTHON_DOCSTRINGS_POINT_SET_3_H

namespace cgalpy {
namespace docstrings {
namespace Point_set_3 {

// CGAL::Point_set_3
constexpr const char* Point_set_3__class__ = "A collection of points with dynamically associated properties.";

constexpr const char* Point_set_3_Property_map = "Model of LvaluePropertyMap with Index as a key type and Type as value type.

Returns: unspecified_type";
constexpr const char* Point_set_3_has_property_map = "tests whether property name of type T already exists.

T type of the property. name Name of the property.

Parameters
----------
name : const std::string &

Returns: bool";
constexpr const char* Point_set_3_add_property_map = "adds a new property name of type T with given default value.

T type of the property. name Name of the property. t Value taken by the
property on already created elements. Returns a pair containing the property
map and a Boolean that is true if the property was added and false if it
already exists (and was therefore not added but only returned).

Parameters
----------
name : const std::string &
t : const T

Returns: std::pair< Property_map < T >, bool >";
constexpr const char* Point_set_3_property_map = "returns the property name of type T .

T type of the property. name Name of the property. Returns an optional
property map (empty if the property was not found).

Parameters
----------
name : const std::string &

Returns: std::optional< Property_map < T > >";
constexpr const char* Point_set_3_remove_property_map = "removes the specified property.

T type of the property. prop The property. Returns true if the property was
removed and false if the property was not found.

Parameters
----------
prop : Property_map < T > &

Returns: bool";
constexpr const char* Point_set_3_has_normal_map = "Convenience method that tests whether the point set has normals.

This method tests whether a property of type Vector and named normal exists.

Returns: bool";
constexpr const char* Point_set_3_add_normal_map = "Convenience method that adds a normal property.

This method adds a property of type Vector and named normal . Returns a pair
containing the normal map and a Boolean that is true if the property was added
and false if it already exists (and was therefore not added but only
returned).

Parameters
----------
default_value : const Vector &

Returns: std::pair< Vector_map , bool >";
constexpr const char* Point_set_3_normal_map = "returns the property map of the normal property.

If the normal property has not been added yet to the point set before calling
this method, the property map is automatically added with add_normal_map() .

Returns: Vector_map";
constexpr const char* Point_set_3_normal_map_1 = "returns the property map of the normal property (constant version).

The normal property must have been added to the point set before calling this
method (see add_normal_map() ).

Returns: const Vector_map";
constexpr const char* Point_set_3_remove_normal_map = "Convenience method that removes the normal property.

Returns true if the property was removed and false if the property was not
found.

Returns: bool";
constexpr const char* Point_set_3_point_map = "returns the property map of the point property.

Returns: Point_map";
constexpr const char* Point_set_3_point_map_1 = "returns the property map of the point property (constant version).

Returns: const Point_map";
constexpr const char* Point_set_3_copy_properties = "Copies the properties from another point set.

All properties from other that do not already exist in this point set are
added and filled to their default values. Properties that exist in both point
sets are left unchanged.

Parameters
----------
other : const Point_set_3 &";
constexpr const char* Point_set_3_properties = "returns a vector with all strings that describe properties.

Returns: std::vector< std::string >";
constexpr const char* Point_set_3_properties_and_types = "returns a vector of pairs that describe properties and associated types.

Returns: std::vector< std::pair< std::string, std::type_index > >";
constexpr const char* Point_set_3_parameters = "returns a sequence of Named Parameters to be used in Point Set Processing algorithms.

The following named parameters are used: point_map : contains the point
property map (see point_map() ) normal_map : contains the normal map (see
normal_map() ) geom_traits : contains the kernel typename Kernel_traits<Point>
:: Kernel this method does not check if the normal map was instantiated or
not. The normal map named parameter should not be used if this property was
not instantiated first.

Returns: unspecified_type";
constexpr const char* Point_set_3_Property_range = "Model of ConstRange that handles constant ranges for property maps with value type Type .

Returns: unspecified_type";
constexpr const char* Point_set_3_range = "returns a property as a range.

Parameters
----------
pmap : const Property_map < T > &

Returns: Property_range < T >";
constexpr const char* Point_set_3_points = "returns a constant range of points.

Returns: Point_range";
constexpr const char* Point_set_3_normals = "returns a constant range of normals.

Returns: Vector_range";
constexpr const char* Point_set_3_Property_back_inserter = "This is an advanced type.

Advanced Model of OutputIterator used to insert elements by defining the value
of the property Property .

Returns: unspecified_type";
constexpr const char* Point_set_3_Push_property_map = "This is an advanced type.

Advanced Model of WritablePropertyMap based on Property and that is allowed to
push new items to the point set if needed.

Returns: unspecified_type";
constexpr const char* Point_set_3_Index_back_inserter = "This is an advanced type.

Advanced Back inserter on indices

Returns: Property_back_inserter < Index_map >";
constexpr const char* Point_set_3_Point_back_inserter = "This is an advanced type.

Advanced Back inserter on points

Returns: Property_back_inserter < Point_map >";
constexpr const char* Point_set_3_Point_push_map = "This is an advanced type.

Advanced Property map for pushing new points

Returns: Push_property_map < Point_map >";
constexpr const char* Point_set_3_Vector_push_map = "This is an advanced type.

Advanced Property map for pushing new vectors

Returns: Push_property_map < Vector_map >";
constexpr const char* Point_set_3_push_property_map = "This is an advanced function.

Advanced returns the push property map of the given property. T type of the
property. prop The property map. Returns a pair containing: the specified
property map and a Boolean set to true or an empty property map and a Boolean
set to false (if the property was not found).

Parameters
----------
prop : Property_map < T > &

Returns: Push_property_map < Property_map < T > >";
constexpr const char* Point_set_3_point_push_map = "This is an advanced function.

Advanced returns the push property map of the point property.

Returns: Point_push_map";
constexpr const char* Point_set_3_normal_push_map = "This is an advanced function.

Advanced returns the push property map of the normal property. The normal
property must have been added to the point set before calling this method (see
add_normal_map() ).

Returns: Vector_push_map";
constexpr const char* Point_set_3_index_back_inserter = "This is an advanced function.

Advanced returns the back inserter on the index property.

Returns: Index_back_inserter";
constexpr const char* Point_set_3_point_back_inserter = "This is an advanced function.

Advanced returns the back inserter on the point property.

Returns: Point_back_inserter";
constexpr const char* Point_set_3_Point_set_3 = "creates an empty point set with no additional property.

with_normal_map true if the normal map should be added. If false (default
value), the normal map can still be added later on (see add_normal_map() ).

Parameters
----------
with_normal_map : bool";
constexpr const char* Point_set_3_operator = "Assignment operator, all properties with their content are copied.

Parameters
----------
ps : const Point_set_3 &

Returns: Point_set_3 &";
constexpr const char* Point_set_3_is_empty = "returns true if the number of elements not marked as removed is 0, false otherwise.

This does not count the removed elements. The method empty() is also available
(see Range ) and does the same thing.

Returns: bool";
constexpr const char* Point_set_3_number_of_points = "returns the number of elements (not counting elements marked as removed).

See number_of_removed_points() for getting the number of elements marked as
removed. The method size() is also available (see Range ) and does the same
thing.

Returns: std::size_t";
constexpr const char* Point_set_3_join = "merges other in the point set.

Shifts the indices of points of other by number_of_points() +
other.number_of_points() . Copies entries of all property maps which have the
same name in the point set and other . Property maps which are only in other
are ignored. If copy_properties() with other as argument is called before
calling this method, then all the content of other will be copied and no
property will be lost in the process. Garbage is collected in both point sets
when calling this method.

Parameters
----------
other : Point_set_3 &

Returns: bool";
constexpr const char* Point_set_3_clear = "Clears the point set properties and content.

After calling this method, the object is the same as a newly constructed
object. The additional properties (such as normal vectors) are also removed
and must thus be re-added if needed.";
constexpr const char* Point_set_3_clear_properties = "Clears all properties created.

After calling this method, all properties are removed. The points are left
unchanged.";
constexpr const char* Point_set_3_reserve = "increases the capacity of internal containers to be able to efficiently accommodate at least s elements

s Expected final number of elements. This method does not change the content
of the point set and is only used for optimization.

Parameters
----------
s : std::size_t";
constexpr const char* Point_set_3_resize = "changes size of the point set.

s Target size of the point set If the given size is larger than the current
size, the capacity of the internal container is extended. If there are element
marked as removed, they may be overwritten. If the given size is smaller than
the current size, garbage is collected and the container is resized.

Parameters
----------
s : std::size_t";
constexpr const char* Point_set_3_insert = "inserts a new element with default property values.

The iterator on the newly added element. If a reallocation happens, all
iterators, pointers and references related to the container are invalidated.
Otherwise, only the end iterator is invalidated, and all iterators, pointers
and references to elements are guaranteed to keep referring to the same
elements they were referring to before the call.

Returns: iterator";
constexpr const char* Point_set_3_insert_1 = "inserts new point with default property values.

p Point to insert Properties of the added point are initialized to their
default value. If a reallocation happens, all iterators, pointers and
references related to the container are invalidated. Otherwise, only the end
iterator is invalidated, and all iterators, pointers and references to
elements are guaranteed to keep referring to the same elements they were
referring to before the call. The iterator on the newly added element.

Parameters
----------
p : const Point &

Returns: iterator";
constexpr const char* Point_set_3_insert_2 = "Convenience method to add a point with a normal vector.

p Point to insert n Associated normal vector Properties of the added point
other than its normal vector are initialized to their default value. If not
already added, a normal property is automatically added to the point set when
using this method. The default value for normal vectors is CGAL::NULL_VECTOR .
If a reallocation happens, all iterators, pointers and references related to
the container are invalidated. Otherwise, only the end iterator is
invalidated, and all iterators, pointers and references to elements are
guaranteed to keep referring to the same elements they were referring to
before the call. The iterator on the newly added element.

Parameters
----------
p : const Point &
n : const Vector &

Returns: iterator";
constexpr const char* Point_set_3_insert_3 = "Convenience method to copy a point with all its properties from another point set.

In the case where two point sets have the same properties, this method allows
the user to easily copy one point (along with the values of all its
properties) from one point set to another. other Point set to which the point
to copy belongs idx Index of the point to copy in other This point set and
other must have the exact same properties, with the exact same names and types
in the exact same order. If a reallocation happens, all iterators, pointers
and references related to the container are invalidated. Otherwise, only the
end iterator is invalidated, and all iterators, pointers and references to
elements are guaranteed to keep referring to the same elements they were
referring to before the call. The iterator on the newly added element.

Parameters
----------
other : const Point_set_3 &
idx : const Index &

Returns: iterator";
constexpr const char* Point_set_3_begin = "returns the begin iterator.

Returns: iterator";
constexpr const char* Point_set_3_end = "returns the past-the-end iterator.

The returned value is the same as garbage_begin() .

Returns: iterator";
constexpr const char* Point_set_3_begin_1 = "returns the begin constant iterator.

Returns: const_iterator";
constexpr const char* Point_set_3_end_1 = "returns the past-the-end constant iterator.

The returned value is the same as garbage_begin() .

Returns: const_iterator";
constexpr const char* Point_set_3_point = "returns a reference to the point corresponding to index .

Parameters
----------
index : const Index &

Returns: Point &";
constexpr const char* Point_set_3_point_1 = "returns a constant reference to the point corresponding to index .

Parameters
----------
index : const Index &

Returns: const Point &";
constexpr const char* Point_set_3_normal = "returns a reference to the normal corresponding to index .

If not already added, a normal property is automatically added to the point
set (see add_normal_map() ).

Parameters
----------
index : const Index &

Returns: Vector &";
constexpr const char* Point_set_3_normal_1 = "returns a constant reference to the normal corresponding to index .

If not already added, a normal property is automatically added to the point
set (see add_normal_map() ).

Parameters
----------
index : const Index &

Returns: const Vector &";
constexpr const char* Point_set_3_remove = "marks all elements between first and last as removed.

The elements are just marked as removed and are not erased from the memory.
collect_garbage() should be called if the memory needs to be disallocated.
Elements can be recovered with cancel_removals() . All iterators, pointers and
references related to the container are invalidated.

Parameters
----------
first : iterator
last : iterator";
constexpr const char* Point_set_3_remove_1 = "marks element specified by iterator as removed.

The element is just marked as removed and is not erased from the memory.
collect_garbage() should be called if the memory needs to be freed. The
element can be recovered with cancel_removals() . The end() iterator is
invalidated, it dereferences to a different element.

Parameters
----------
it : iterator";
constexpr const char* Point_set_3_remove_2 = "marks element specified by Index as removed.

The element is just marked as removed and is not erased from the memory.
collect_garbage() should be called if the memory needs to be freed. The
element can be recovered with cancel_removals() . The end() iterator is
invalidated, it dereferences to a different element.

Parameters
----------
index : const Index &";
constexpr const char* Point_set_3_is_removed = "returns true if the element is marked as removed, false otherwise.

When iterating between begin() and end() , no element marked as removed can be
found.

Parameters
----------
it : const_iterator

Returns: bool";
constexpr const char* Point_set_3_is_removed_1 = "returns true if the element is marked as removed, false otherwise.

When iterating between begin() and end() , no element marked as removed can be
found.

Parameters
----------
index : const Index &

Returns: bool";
constexpr const char* Point_set_3_garbage_begin = "returns the constant iterator to the first element marked as removed (equal to garbage_end() if no elements are marked as removed.

Returns: const_iterator";
constexpr const char* Point_set_3_garbage_end = "returns the past-the-end constant iterator of the elements marked as removed.

Returns: const_iterator";
constexpr const char* Point_set_3_number_of_removed_points = "Number of removed points.

garbage_size()

Returns: std::size_t";
constexpr const char* Point_set_3_garbage_size = "Number of removed points.

number_of_removed_points()

Returns: std::size_t";
constexpr const char* Point_set_3_has_garbage = "returns true if there are elements marked as removed, false otherwise.

Returns: bool";
constexpr const char* Point_set_3_collect_garbage = "erases from memory the elements marked as removed.";
constexpr const char* Point_set_3_cancel_removals = "restores all removed points.

After removing one or several points, calling this method restores the point
set to its initial state: points that were removed (and their associated
properties) are restored. This method is only guaranteed to work if no point
was inserted after the removal: otherwise, some points might not be restored.
If collect_garbage() was called after removal, the points are irremediably
lost and nothing will be restored.";
constexpr const char* Point_set_3_Point_3 = "The point type.

Returns: Point";
constexpr const char* Point_set_3_Vector_3 = "The vector type.

Returns: Vector";
constexpr const char* Point_set_3_iterator = "Iterator type of the point set with value type Index is model of RandomAccessIterator .

Returns: unspecified_type";
constexpr const char* Point_set_3_const_iterator = "Constant iterator type of the point set with value type Index is model of RandomAccessIterator .

Returns: unspecified_type";
constexpr const char* Point_set_3_Point_map = "Property map of points.

Returns: Property_map < Point >";
constexpr const char* Point_set_3_Vector_map = "Property map of vectors.

Returns: Property_map < Vector >";
constexpr const char* Point_set_3_Point_range = "Constant range of points.

Returns: Property_range < Point >";
constexpr const char* Point_set_3_Vector_range = "Constant range of vectors.

Returns: Property_range < Vector >";
constexpr const char* Point_set_3_operator_1 = "Append other at the end of ps .

Shifts the indices of points of other by ps.number_of_points() +
other.number_of_points() . Copies entries of all property maps which have the
same name in ps and other . Property maps which are only in other are ignored.
Garbage is collected in both point sets when calling this method.

Parameters
----------
ps : Point_set_3 < Point, Vector > &
other : Point_set_3 < Point, Vector > &

Returns: Point_set_3 < Point, Vector > &";


constexpr const char* Point_set_3_operator_2 = "Append other at the end of ps .

Shifts the indices of points of other by ps.number_of_points() +
other.number_of_points() . Copies entries of all property maps which have the
same name in ps and other . Property maps which are only in other are ignored.
Garbage is collected in both point sets when calling this method.

Parameters
----------
ps : Point_set_3 < Point, Vector > &
other : Point_set_3 < Point, Vector > &

Returns: Point_set_3 < Point, Vector > &";


// std
constexpr const char* std__class__ = "STL namespace.";

} // namespace Point_set_3
} // namespace docstrings
} // namespace cgalpy

#endif // CGAL_PYTHON_DOCSTRINGS_POINT_SET_3_H
