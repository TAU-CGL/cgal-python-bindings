\begin{frame}[fragile]
  \frametitle{\cmake}
  \begin{itemize}
  \item The bindings are wrapper libraries that bridge C++ and Python
  \item We use \cmake{} to generate the binding libraries
  \item The content of the libraries and their names are governed by flags
    provided to cmake
  \end{itemize}
\end{frame}
