\begin{frame}[fragile]
  \frametitle{\cmake}
  \begin{itemize}
  \item Bindings are wrapper libraries that bridge C++ and Python
  \item We use \cmake{} to generate the binding libraries
  \item The content of the libraries and their names are governed by flags
    provided to \myLstinline{cmake}
  \end{itemize}

  \def\filename{epec_fixed_release.cmake}%
  \lstinputlisting[style=cmakeFramedStyle,basicstyle=\tiny,
                   label={lst:cmake:epecFixed},
                   caption={\cmake{} flag settings used to generate bindings for the exact-predicates and exact-constructions kernel.}]
                  {\cmakeDir\filename}
\end{frame}
